columns:
  user_id: user_id
  item_id: song_id
  timestamp: ts

preprocessing:
  min_user_interactions: 10
  min_item_interactions: 5

split:
  test_window_days: 14
  gap_days: 0
  strategy: temporal_window
